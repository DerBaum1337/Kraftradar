---
interface Props {
	partner: 'amazon' | 'hsn';
	url: string;
	linkText: string;
}

const { partner, url, linkText } = Astro.props;
const partnerName = partner === 'amazon' ? 'Amazon' : 'HSN';
const disclosure =
	partner === 'amazon'
		? 'Als Amazon-Partner verdiene ich an qualifizierten Verkäufen. Die mögliche Provision wird von Amazon gezahlt; durch die Nutzung des Links entstehen dir keine zusätzlichen Kosten.'
		: 'Wenn du über diesen Link kaufst, kann ich eine Provision erhalten. Die mögliche Provision wird von HSN gezahlt; durch die Nutzung des Links entstehen dir keine zusätzlichen Kosten.';
---

<aside class="affiliate-product-box" aria-label={`Werbung: ${linkText} bei ${partnerName}`}>
	<p class="affiliate-product-box__label">Werbung / Affiliate-Link</p>
	<h2>{linkText} bei {partnerName}</h2>
	<p>{disclosure}</p>
	<a
		class="button affiliate-product-box__button"
		href={url}
		target="_blank"
		rel="sponsored noopener noreferrer"
		aria-label={`${linkText} bei ${partnerName} ansehen (öffnet in einem neuen Tab)`}
	>
		Bei {partnerName} ansehen <span aria-hidden="true">↗</span>
	</a>
</aside>
