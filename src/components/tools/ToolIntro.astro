---
interface Props {
	eyebrow?: string;
	title: string;
	description: string;
}

const { eyebrow = 'KraftRadar Tools', title, description } = Astro.props;
---

<section class="tool-hero">
	<p class="eyebrow">{eyebrow}</p>
	<h1>{title}</h1>
	<p>{description}</p>
</section>
